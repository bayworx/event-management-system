framework:
    messenger:
        routing:
            # In production, send emails asynchronously to avoid blocking HTTP requests
            Symfony\Component\Mailer\Messenger\SendEmailMessage: async
