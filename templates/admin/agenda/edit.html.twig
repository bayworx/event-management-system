{% extends 'admin/base.html.twig' %}

{% block title %}Edit Agenda Item - {{ agenda_item.title }}{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h1 class="h3 mb-2">Edit Agenda Item</h1>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="{{ path('admin_dashboard') }}">Dashboard</a></li>
                            <li class="breadcrumb-item"><a href="{{ path('admin_event_index') }}">Events</a></li>
                            <li class="breadcrumb-item"><a href="{{ path('admin_event_show', {id: agenda_item.event.id}) }}">{{ agenda_item.event.title }}</a></li>
                            <li class="breadcrumb-item"><a href="{{ path('admin_agenda_index', {eventId: agenda_item.event.id}) }}">Agenda</a></li>
                            <li class="breadcrumb-item active" aria-current="page">Edit Item</li>
                        </ol>
                    </nav>
                </div>
                <div>
                    <a href="{{ path('admin_agenda_show', {id: agenda_item.id}) }}" class="btn btn-outline-info me-2">
                        <i class="fas fa-eye me-1"></i> View Item
                    </a>
                    <a href="{{ path('admin_agenda_index', {eventId: agenda_item.event.id}) }}" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left me-1"></i> Back to Agenda
                    </a>
                </div>
            </div>

            <div class="row">
                <div class="col-lg-8">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="mb-0">
                                <i class="fas fa-edit me-2"></i>Edit: {{ agenda_item.title }}
                            </h5>
                        </div>
                        <div class="card-body">
                            {{ form_start(form) }}
                                <div class="row">
                                    <div class="col-md-12">
                                        <div class="mb-3">
                                            {{ form_label(form.title, null, {'label_attr': {'class': 'form-label required'}}) }}
                                            {{ form_widget(form.title, {'attr': {'class': 'form-control', 'placeholder': 'Enter agenda item title...'}}) }}
                                            {{ form_errors(form.title) }}
                                        </div>
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-md-6">
                                        <div class="mb-3">
                                            {{ form_label(form.itemType, null, {'label_attr': {'class': 'form-label'}}) }}
                                            {{ form_widget(form.itemType, {'attr': {'class': 'form-select'}}) }}
                                            {{ form_errors(form.itemType) }}
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mb-3">
                                            {{ form_label(form.isVisible, null, {'label_attr': {'class': 'form-label'}}) }}
                                            <div class="form-check form-switch">
                                                {{ form_widget(form.isVisible, {'attr': {'class': 'form-check-input', 'role': 'switch'}}) }}
                                                <label class="form-check-label" for="{{ form.isVisible.vars.id }}">
                                                    Show this item to attendees
                                                </label>
                                            </div>
                                            {{ form_errors(form.isVisible) }}
                                        </div>
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-md-6">
                                        <div class="mb-3">
                                            {{ form_label(form.startTime, null, {'label_attr': {'class': 'form-label'}}) }}
                                            {{ form_widget(form.startTime, {'attr': {'class': 'form-control'}}) }}
                                            {{ form_errors(form.startTime) }}
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mb-3">
                                            {{ form_label(form.endTime, null, {'label_attr': {'class': 'form-label'}}) }}
                                            {{ form_widget(form.endTime, {'attr': {'class': 'form-control'}}) }}
                                            {{ form_errors(form.endTime) }}
                                        </div>
                                    </div>
                                </div>

                                <div class="mb-3">
                                    {{ form_label(form.description, null, {'label_attr': {'class': 'form-label'}}) }}
                                    {{ form_widget(form.description, {'attr': {'class': 'form-control', 'rows': 4, 'placeholder': 'Enter a detailed description...'}}) }}
                                    {{ form_errors(form.description) }}
                                </div>

                                <div class="row">
                                    <div class="col-md-6">
                                        <div class="mb-3">
                                            {{ form_label(form.speaker, null, {'label_attr': {'class': 'form-label'}}) }}
                                            {{ form_widget(form.speaker, {'attr': {'class': 'form-control', 'placeholder': 'Speaker name...'}}) }}
                                            {{ form_errors(form.speaker) }}
                                            <div class="form-text">Use this field for external speakers</div>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mb-3">
                                            {{ form_label(form.presenter, null, {'label_attr': {'class': 'form-label'}}) }}
                                            {{ form_widget(form.presenter, {'attr': {'class': 'form-select'}}) }}
                                            {{ form_errors(form.presenter) }}
                                            <div class="form-text">Select from registered presenters</div>
                                        </div>
                                    </div>
                                </div>

                                <div class="mb-3">
                                    {{ form_label(form.location, null, {'label_attr': {'class': 'form-label'}}) }}
                                    {{ form_widget(form.location, {'attr': {'class': 'form-control', 'placeholder': 'Room, building, or virtual link...'}}) }}
                                    {{ form_errors(form.location) }}
                                </div>

                                <div class="mb-4">
                                    {{ form_label(form.sortOrder, null, {'label_attr': {'class': 'form-label'}}) }}
                                    {{ form_widget(form.sortOrder, {'attr': {'class': 'form-control', 'min': '0'}}) }}
                                    {{ form_errors(form.sortOrder) }}
                                    <div class="form-text">Lower numbers appear first in the agenda</div>
                                </div>

                                <div class="d-flex justify-content-between">
                                    <a href="{{ path('admin_agenda_index', {eventId: agenda_item.event.id}) }}" class="btn btn-outline-secondary">
                                        <i class="fas fa-times me-1"></i> Cancel
                                    </a>
                                    <button type="submit" class="btn btn-primary">
                                        <i class="fas fa-save me-1"></i> Update Agenda Item
                                    </button>
                                </div>
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>

                <div class="col-lg-4">
                    <div class="card">
                        <div class="card-header">
                            <h6 class="mb-0">
                                <i class="fas fa-info-circle me-2"></i>Item Details
                            </h6>
                        </div>
                        <div class="card-body">
                            <div class="small">
                                <p><strong>Created:</strong> {{ agenda_item.createdAt|date('F j, Y g:i A') }}</p>
                                {% if agenda_item.updatedAt %}
                                    <p><strong>Updated:</strong> {{ agenda_item.updatedAt|date('F j, Y g:i A') }}</p>
                                {% endif %}
                                <p><strong>Status:</strong> 
                                    <span class="badge bg-{{ agenda_item.visible ? 'success' : 'secondary' }}">
                                        {{ agenda_item.visible ? 'Visible' : 'Hidden' }}
                                    </span>
                                </p>
                                {% if agenda_item.itemType %}
                                    <p><strong>Type:</strong> <span class="badge bg-info">{{ agenda_item.itemType|title }}</span></p>
                                {% endif %}
                                <p class="mb-0"><strong>Sort Order:</strong> {{ agenda_item.sortOrder }}</p>
                            </div>
                        </div>
                    </div>

                    <div class="card mt-3">
                        <div class="card-header">
                            <h6 class="mb-0">
                                <i class="fas fa-info-circle me-2"></i>Tips
                            </h6>
                        </div>
                        <div class="card-body">
                            <div class="small">
                                <h6>Agenda Item Types:</h6>
                                <ul class="list-unstyled mb-3">
                                    <li><strong>Presentation:</strong> Main talks or presentations</li>
                                    <li><strong>Break:</strong> Coffee breaks, lunch, etc.</li>
                                    <li><strong>Workshop:</strong> Interactive sessions</li>
                                    <li><strong>Panel:</strong> Panel discussions</li>
                                    <li><strong>Networking:</strong> Social activities</li>
                                    <li><strong>Other:</strong> Any other type of activity</li>
                                </ul>

                                <h6>Speaker vs Presenter:</h6>
                                <ul class="list-unstyled mb-3">
                                    <li><strong>Speaker:</strong> External speakers (text field)</li>
                                    <li><strong>Presenter:</strong> Registered users/presenters</li>
                                </ul>

                                <h6>Visibility:</h6>
                                <p class="mb-0">Hidden items won't appear in the public agenda but will still be visible to administrators.</p>
                            </div>
                        </div>
                    </div>

                    <div class="card mt-3">
                        <div class="card-header">
                            <h6 class="mb-0">
                                <i class="fas fa-calendar me-2"></i>Event Information
                            </h6>
                        </div>
                        <div class="card-body">
                            <div class="small">
                                <p><strong>Event:</strong> {{ agenda_item.event.title }}</p>
                                <p><strong>Date:</strong> {{ agenda_item.event.startDate|date('F j, Y') }}</p>
                                <p><strong>Time:</strong> {{ agenda_item.event.startDate|date('g:i A') }}
                                {% if agenda_item.event.endDate %}
                                    - {{ agenda_item.event.endDate|date('g:i A') }}
                                {% endif %}</p>
                                {% if agenda_item.event.location %}
                                    <p class="mb-0"><strong>Location:</strong> {{ agenda_item.event.location }}</p>
                                {% endif %}
                            </div>
                        </div>
                    </div>

                    <div class="card mt-3">
                        <div class="card-header">
                            <h6 class="mb-0">
                                <i class="fas fa-cogs me-2"></i>Actions
                            </h6>
                        </div>
                        <div class="card-body">
                            <div class="d-grid gap-2">
                                <form method="post" action="{{ path('admin_agenda_toggle_visibility', {id: agenda_item.id}) }}" class="d-inline">
                                    <input type="hidden" name="_token" value="{{ csrf_token('toggle-visibility-' ~ agenda_item.id) }}">
                                    <button type="submit" class="btn btn-sm btn-outline-secondary w-100">
                                        <i class="fas fa-{{ agenda_item.visible ? 'eye-slash' : 'eye' }} me-2"></i>
                                        {{ agenda_item.visible ? 'Hide Item' : 'Show Item' }}
                                    </button>
                                </form>
                                
                                <form method="post" action="{{ path('admin_agenda_duplicate', {id: agenda_item.id}) }}" class="d-inline">
                                    <input type="hidden" name="_token" value="{{ csrf_token('duplicate-agenda-' ~ agenda_item.id) }}">
                                    <button type="submit" class="btn btn-sm btn-outline-info w-100">
                                        <i class="fas fa-copy me-2"></i> Duplicate Item
                                    </button>
                                </form>

                                <hr>

                                <form method="post" action="{{ path('admin_agenda_delete', {id: agenda_item.id}) }}" class="d-inline delete-form">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete-agenda-' ~ agenda_item.id) }}">
                                    <button type="submit" class="btn btn-sm btn-outline-danger w-100">
                                        <i class="fas fa-trash me-2"></i> Delete Item
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Auto-calculate end time when start time changes
            const startTimeInput = document.querySelector('input[id$="_startTime"]');
            const endTimeInput = document.querySelector('input[id$="_endTime"]');
            
            if (startTimeInput && endTimeInput) {
                startTimeInput.addEventListener('change', function() {
                    if (this.value && !endTimeInput.value) {
                        // Add 1 hour to start time for default end time
                        const startDate = new Date(this.value);
                        startDate.setHours(startDate.getHours() + 1);
                        
                        // Format to match datetime-local input format
                        const year = startDate.getFullYear();
                        const month = String(startDate.getMonth() + 1).padStart(2, '0');
                        const day = String(startDate.getDate()).padStart(2, '0');
                        const hours = String(startDate.getHours()).padStart(2, '0');
                        const minutes = String(startDate.getMinutes()).padStart(2, '0');
                        
                        endTimeInput.value = `${year}-${month}-${day}T${hours}:${minutes}`;
                    }
                });
            }

            // Delete confirmation
            document.querySelector('.delete-form')?.addEventListener('submit', function(e) {
                e.preventDefault();
                
                if (confirm('Are you sure you want to delete this agenda item? This action cannot be undone.')) {
                    this.submit();
                }
            });
        });
    </script>
{% endblock %}